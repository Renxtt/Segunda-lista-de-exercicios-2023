#include <stdlib.h>
#include <math.h>

#include <stdio.h>

main(void){
	int raiz, nub, resu;
	
	
	printf("qual o numero dessa raiz:");
	scanf("%d", &nub);
	resu = sqrt(nub);
	printf("%d", resu);
	
	
}
